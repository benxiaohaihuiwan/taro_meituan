<block wx:if="{{$taroCompReady}}">
    <view class="head">
        <top></top>
        <image class="back" src="{{anonymousState__temp}}"></image>
        <view class="store">
            <image class="store_img" src="{{anonymousState__temp2}}"></image>
            <view class="store_text"><text class="title">{{store.title}}</text><text>{{store.notice}}</text>
                <view><text class="tags_text" wx:key="index" wx:for="{{store.tags}}" wx:for-item="item" wx:for-index="index">{{item}}</text>
                </view>
            </view>
        </view>
        <activity></activity>
    </view>
</block>